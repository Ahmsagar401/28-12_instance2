To print length of an array and elements of array
Updating in local machine
#!/bin/bash
array=(10 20 30 40 50)
length=${#array[@]}
echo "Length of array is $length"
echo -e "Elements of an array are \n ${array[*]}"
